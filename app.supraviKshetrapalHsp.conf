server {
    listen 80;
    #server_name app.supravi.ai;
    server_name 172.16.1.101;

    # Redirect to HTTPS for requests not for the specified endpoints
    location / {
        return 301 https://$host$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certot;
        try_files $uri =404;
    }

    location ~ ^/(instances|transfers|studies|patients)(/.*)?$ {
        # Capture the endpoint (instances, transfers, or studies) and pass it to the proxy
        set $endpoint $1;

        # Capture the rest of the URI and append it to the endpoint
        set $remainder $2;

        # Append a trailing slash if $remainder is empty
        if ($remainder = "") {
            set $remainder "/";
        }

        # Proxy to the combined endpoint
        proxy_pass http://172.17.0.1:8042/$endpoint$remainder;

        # Proxy settings
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        # CORS headers
        add_header Access-Control-Allow-Origin "$http_origin";
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Origin,authorization, X-Requested-With, Content-Type, Accept";

        # Allow OPTIONS requests
        if ($request_method = OPTIONS) {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE';
            add_header 'Access-Control-Allow-Headers' 'Origin, Content-Type, Accept, Authorization';
            return 200;
        }
    }
}

server {
    listen 443 ssl;

    #server_name app.supravi.ai;
    server_name 172.16.1.101;
    ssl_certificate /etc/letsencrypt/live/app.supravi.ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.supravi.ai/privkey.pem;
    client_max_body_size 250M;
    access_log /var/log/nginx/app.log;
    root /var/www/build;
    index index.html index.htm;
    try_files $uri /index.html;

    #    location = /viewer {
    #      return 301 /viewer/;
    #    }

    #    location /viewer/ {
    #       alias /var/www/viewer/;
    #       index index.html;
    #       try_files $uri $uri/ /viewer/index.html;
    #       add_header Access-Control-Allow-Origin "$http_origin";
    #       add_header Cross-Origin-Opener-Policy same-origin;
    #       add_header Cross-Origin-Embedder-Policy require-corp;
    #    }
    location /viewer {
        root /var/www/;

        # Disable caching by setting Cache-Control header
        #add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
        #add_header Pragma "no-cache";
        #add_header Expires "0";

        add_header Access-Control-Allow-Origin "$http_origin";
        add_header Cross-Origin-Opener-Policy same-origin;
        add_header Cross-Origin-Embedder-Policy require-corp;
    }


    location /dicom-web {
        proxy_pass http://172.17.0.1:8042/dicom-web;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        add_header Access-Control-Allow-Origin "$http_origin";
        add_header 'Access-Control-Allow_Credentials' 'true';
        add_header 'Access-Control-Allow-Headers' 'Authorization,Accept,Origin,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Content-Range,Range';
        add_header 'Access-Control-Allow-Methods' 'GET,POST,OPTIONS,PUT,DELETE,PATCH';
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header Authorization "Basic cGFjc2FkbWluOmxrc2okODc2MzQ1";
    }

    # DICOM-web2 location (handled by app on port 8000)
    location /proxy/ {
        proxy_pass http://172.17.0.1:8000/;

        #add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
        #add_header Pragma "no-cache";
        #add_header Expires "0";

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # mongoBackup location (handled by app on port 5002)
    location /mongoBackup/ {
        proxy_pass http://172.17.0.1:5002/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # pdf-generator location (handled by app on port 5002)
    location /pdfgen/ {
        proxy_pass http://172.17.0.1:6000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /uploadDicom/ {
        proxy_pass http://172.17.0.1:5050/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }


    location /dicom-cases {
        proxy_pass http://172.17.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/ {
        proxy_pass http://172.17.0.1:4000;

        # Recommended headers for Next.js static assets
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Optional: caching for static files
        proxy_cache_bypass $http_upgrade;
        proxy_http_version 1.1;
    }

    location /_next/ {
        proxy_pass http://172.17.0.1:4000;

        # Recommended headers for Next.js static assets
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Optional: caching for static files
        proxy_cache_bypass $http_upgrade;
        proxy_http_version 1.1;
    }


    location ~ ^/(instances|transfers|studies|patients)(/.*)?$ {
        # Capture the endpoint (instances, transfers, or studies) and pass it to the proxy
        set $endpoint $1;

        # Capture the rest of the URI and append it to the endpoint
        set $remainder $2;

        # Append a trailing slash if $remainder is empty
        if ($remainder = "") {
            set $remainder "/";
        }

        # Proxy to the combined endpoint
        proxy_pass http://172.17.0.1:8042/$endpoint$remainder;

        # Proxy settings
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        # CORS headers
        add_header Access-Control-Allow-Origin "$http_origin";
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Origin,authorization, X-Requested-With, Content-Type, Accept";

        # Allow OPTIONS requests
        if ($request_method = OPTIONS) {
            return 200;
        }
    }

    location /node/ {
        proxy_pass http://172.17.0.1:5500/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Origin,authorization, X-Requested-With, Content-Type, Accept, Type";
        # Allow OPTIONS requests
        #if ($request_method = OPTIONS) {
        #    add_header 'Access-Control-Allow-Origin' '*';
        #    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE';
        #    add_header 'Access-Control-Allow-Headers' 'Origin, Content-Type, Accept, Authorization';
        #    return 200;
        #}
    }
    location /downloads/ {
        alias /var/www/downloads/;

        auth_basic "Downloads";
        auth_basic_user_file /etc/nginx/.htpasswd;

        autoindex on;
    }
}